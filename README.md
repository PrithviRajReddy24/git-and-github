Welcomt to my git and github repository

     mkdir mygitrepos
     cd mygitrepos/
     sudo apt-get install git
     git --version
     github auth login
     git-hub auth login
     gh auth login
     sudo apt-get install gh
     gh auth login
   

